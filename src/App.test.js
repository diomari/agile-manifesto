import { render, screen } from '@testing-library/react';
import App from './App';

test('fetch and renders list data', () => {
  // Pass
});


// Fetches and renders values

// Fetches and renders principles
